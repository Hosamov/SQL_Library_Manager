<!DOCTYPE html>
html(lang="en" dir="ltr")
  head
    meta(charset='utf-8')
    meta(http-equiv='x-ua-compatible', content='ie=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title ERROR
    link(rel='stylesheet', href='/static/stylesheets/style.css')

  body

    block content
      div.error-container
        h1= `Server Error`
        h2= `Error status code: ${err.status} - Server Error`
        p.error-p= `Oops! It looks like something went wrong on the server...`
        
        a.home(href='/')
          button.home-button(type="button") Return to Homepage

        div.error-stack

          pre #{err.stack}
